"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readFinancialLayout = readFinancialLayout;
exports.writeFinancialLayout = writeFinancialLayout;
const node_fs_1 = __importDefault(require("node:fs"));
const node_path_1 = __importDefault(require("node:path"));
const LAYOUT_PATH = node_path_1.default.join(__dirname, "../../prisma/seedData/financialLayout.json");
const DEFAULT_LAYOUT = {
    sections: { sales: true, purchases: true, expenses: true, netIncome: true },
    showDailyTotals: true,
    showSummaryCards: true,
    header: { title: "Financial Report", position: "center" },
    footer: { text: "Generated by system", position: "center" },
};
let cache = null;
let flushTimer = null;
const FLUSH_DELAY_MS = 500;
function ensureDir() {
    const dir = node_path_1.default.dirname(LAYOUT_PATH);
    if (!node_fs_1.default.existsSync(dir))
        node_fs_1.default.mkdirSync(dir, { recursive: true });
}
function readFinancialLayout() {
    try {
        if (cache)
            return cache;
        ensureDir();
        if (!node_fs_1.default.existsSync(LAYOUT_PATH)) {
            cache = DEFAULT_LAYOUT;
            return cache;
        }
        const raw = node_fs_1.default.readFileSync(LAYOUT_PATH, "utf-8");
        const data = JSON.parse(raw || "{}");
        cache = { ...DEFAULT_LAYOUT, ...(data || {}) };
        return cache;
    }
    catch {
        cache = DEFAULT_LAYOUT;
        return cache;
    }
}
function writeFinancialLayout(next) {
    cache = { ...DEFAULT_LAYOUT, ...(next || {}) };
    ensureDir();
    if (flushTimer)
        clearTimeout(flushTimer);
    flushTimer = setTimeout(() => {
        try {
            node_fs_1.default.writeFileSync(LAYOUT_PATH, JSON.stringify(cache, null, 2), "utf-8");
        }
        catch {
            // ignore write errors
        }
    }, FLUSH_DELAY_MS);
}
